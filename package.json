{
  "name": "trip-planner",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve -s dist -l $PORT"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "server": "^1.0.42"
  }
}
